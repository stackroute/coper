/*const chai = require('chai');
const expect = chai.expect;
describe('Test plan for Utterence Receiver', function() {
it('Testing for processing an incomplete Utterance Text', function(done){
	this.timeout(5000);

        const analyzer = require('./');
        let userName = 'abc';

        let conversation = {
            "activity": "SCRUM"
        };
        let utteranceText = 'create project lucy';
        analyzer.processUtterance(userName, convStartTime, utteranceText, function(err, analysisRes){
        	console.log("processUtterance result: ", JSON.stringify(analysisRes));
//expect element
        },function(err)
	        {
	        	console.log(err);
	        });
}
}*/